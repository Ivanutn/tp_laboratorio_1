/** \brief la funcionSumar se encarga de recibir dos numeros y devolver el resultado.
 *
 * \param float numero A
 * \param float numero B
 * \return float Resultado
 *
 */
float funcionSumar(float,float);
/** \brief la funcionRestar se encarga de recibir dos numeros y devolver el resultado.
 *
 * \param float numero A
 * \param float numero B
 * \return float Resultado
 *
 */
float funcionRestar(float,float);
/** \brief la funcionMultiplicar se encarga de recibir dos numeros y devolver el resultado.
 *
 * \param float numero A
 * \param float numero B
 * \return float Resultado
 *
 */
float funcionMultiplicar(float,float);
/** \brief la funcionDividir se encarga de recibir dos numeros y devolver el resultado.
 *         se va a contemplar si el segundo numero que se ingresa es un cero.
 *         Si el segundo numero es un cero la funcion devolvera un cero, caso contrario, devolvera el resultado.
 * \param float numero A
 * \param float numero B
 * \return float Resultado
 *
 */
float funcionDividir(float,float);
/** \brief la funcionFactorial se encarga de recibir un numero y devolver el resultado.
 *         se conte
 * \param float numero
 * \return float
 *
 */
float funcionFactorial(float);
/** \brief la funcion pedirNumero se encarga de pedirle al usuario que ingrese un numero y que este sea retornado
 *
 * \param void
 * \return float numero
 *
 */
float pedirNumero(void);
void menu(void);
/** \brief la funcion determinarIntFloat se encarga de recibir un numero decimal y devolver un cero si un decimal con coma,
 *         caso contrario devolvera 1 si es un entero.
 * \param float
 * \return int
 *
 */
int determinarIntFloat(float);
